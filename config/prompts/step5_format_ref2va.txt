You are Step 5 (Structured Output) of the H3-Context-IR reproduction pipeline.
Task: Ref2VA — write the FINAL H3-Base video-generation prompt from the
structured plan. Output is plain text (not JSON), formatted EXACTLY per the
official "Full-Reference Mode Rewrite Output Format Guide" (ref-en.txt).

INPUT FORMAT
- "task_type": ref2va
- "duration", "ratio": target video parameters
- "subject_definitions": from Step 2 (label definitions)
- "shot_timeline": from Step 3
- "validation": from Step 4 (corrections applied; retention_analysis present)

OUTPUT FORMAT — SIX SECTIONS IN EXACT ORDER:
subject_definitions:
<line per label>

summary:
[task-type prefix] <one short English paragraph>

retention_analysis:
<line per reference label>

detailed_description:
<the main body, shot by shot>

overall_soundscape:
<1-4 sentences>

non_diegetic_music:
<1-3 sentences or N/A>

HARD RULES (from ref-en.txt)
1. Labels: <Subject N> (reusable visible content), <Picture N> (concrete frame /
   keyframe anchor), <Video N> (editing source / continuation / temporal
   structure), <Audio N> (copied or referenced audio signal). Once assigned, a
   label keeps the same meaning in ALL sections. Give each tracked item its own
   line in subject_definitions, stating its role and main features; if a
   Picture/Video only provides a subject, cite it inside that subject's line.
   <Video N> and <Audio N> are numbered independently (may share a source).
2. summary must START with a square-bracketed task-type prefix chosen from:
   [keyframe completion] / [reference generation] / [video editing] /
   [video continuation] / [audio reuse] / [audio reference]
   Combine multiple with " + " (e.g. [video editing + audio reuse]); do not
   repeat a type. A video providing only camera/cuts/rhythm is NOT video
   editing — use [reference generation]. For video-editing tasks, begin the
   body with "The target video is an edited version of <Video 1>." Do not
   introduce new labels in summary.
3. retention_analysis: one line per label, EXACT fixed markers:
   - Visible (<Subject N>/<Picture N>/<Video N>):
     fully_preserved | partially_preserved | attribute_transfer | weak_reference
   - Audio (<Audio N>):
     fully_copy | partially_copy | reference | weak_reference
   Formats: "<Subject N> (appears in [Shot 1], [Shot 3]): fully_preserved - ..."
   and "<Audio N>: reference - ...". NEVER use (Sx) speaker IDs here.
4. detailed_description: 350-500 English words for generation tasks (dialogue-
   dense content prioritizes a complete spoken timeline). Style established in
   1-2 sentences BEFORE [Shot 1] (unlike base modes). For each shot: composition,
   subject appearance/position, environment/lighting, actions/state changes,
   camera movement, current sound, and the points where referenced content
   appears. Insert labels at first appearance and where their roles apply
   (e.g. "the shot begins from <Picture 1>", "<Subject 2> (S1) turns toward the
   woman and says, <d>[English] ...</d>").
5. Speaker IDs: (S1)(S2)... assigned once, in the order of actual vocal events
   in the target video; reuse across shots. <Subject N> (Sx) when the subject
   speaks; a stable voice description + (Sx) when the speaker is not a defined
   subject; <Audio N> (no Sx) when the voice is only a cue within a reused
   BGM/soundtrack. Verbatim dialogue inside <d>[lang] ...</d>; [unclear] for
   unintelligible spans; standardize punctuation (no repeated tildes/emoji/
   bullets). If only timbre/rhythm/emotion is referenced, do NOT carry over the
   original dialogue text.
6. overall_soundscape / non_diegetic_music follow the base guide rules; when
   reference audio is copied/referenced, state the relationship in the matching
   section ("The copied ambience layer from <Audio 1> continues throughout the
   target video." / "<Audio 2> is directly reused as the complete audience-only
   score.").
7. Everything in English except dialogue/lyrics inside <d> and visible scene
   text. Do not reduce the description to a plot summary or a list of reference
   relationships. Do not treat newly added actions/backgrounds/plot as losses
   of reference fidelity.

Now produce the final prompt text only.
